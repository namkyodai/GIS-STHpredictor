Note:

Under the main program shapefiles are stored in root forlder and in originalfile and output folder

1) the originalfile folder contains one python program to create the input shapefiles, which is used in the main program.

within the root folder, there are several python files used to define the function used within the main program.

shapefile.py is used to read the shapefile

prediction.py contain the function to calculate the evolution of prevalence given the input value such as time, breakpoint, initial condition state and the transition probability

mo4function.py contains pre-defined function used for module 4. 

